{% extends layout_template  %}

{% block title %}Wachtwoord vergeten?{% endblock %}

{% block content %}

    <h1>Wachtwoord vergeten?</h1>

    <p class="text-muted lead">
        Geef je emailadres op en we sturen je instructies waarmee je een nieuw wachtwoord in kan stellen.
    </p>

    {% if error %}
        <div class="alert alert-danger">{{ error|nl2br }}</div>
    {% endif %}

    <form class="form-horizontal" method="POST" action="{{ path('user.forgot-password') }}">
        <div class="form-group">
            <label class="col-sm-2 control-label" for="inputEmail">Je email-adres</label>
            <div class="col-sm-8">
                <input class="form-control" name="email" type="text" id="inputEmail" required value="{{ email }}">
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-8 col-sm-offset-2">
                <button type="submit" class="btn btn-primary">Mail me de instructies</button>
                <a href="{{ path('user.login') }}" style="margin-left: 10px;">Terug naar de inlog-pagina</a>
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-8 col-sm-offset-2" style="margin-top: 20px;">
                <hr/>
                Krijg je geen mail binnen enkele minuten, check dan je spamfolder.
                De afzender is <strong>{{ fromAddress }}</strong>.
            </div>
        </div>
    </form>


{% endblock %}
