{% extends "layout.html.twig" %}

{% block content %}

    <div class="row">
        <div class="col-md-12">
            <h1>Inhoud van de dataset</h1>
            <p>Je werkt in dataset "<a href="/datasets/{{ dataset.id }}">{{ dataset.name }}</a>"</p>

            {{ table|raw }}
        </div>

    </div>

{% endblock %}