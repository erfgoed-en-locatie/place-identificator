{% extends "layout.html.twig" %}

{% block content %}

    <h1>Configuratie voor dataset "{{ dataset.name }}"</h1>
    <div class="row">
        <div class="col-md-6">
            <h2>Van...</h2>

            <p>Geef hieronder aan welke kolommen van het csv-bestand gebruikt moeten worden voor de het
                standaardiseren.</p>

            {% form_theme form 'bootstrap_3_layout.html.twig' %}

            {{ form_start(form) }}

            {{ form_row(form.placename) }}
            <p class="alert-danger">uitlegregeltje dat je hier de te standaardiseren term moet uitkiezen</p>

            {{ form_row(form.liesin) }}
            <p class="alert-danger">uitlegregeltje dat je hier alleen iets hoeft in te vullen als je een tweede kolom
                hebt ....</p>


            {{ form_row(form.hg_type) }}
            <p class="alert-danger">uitleg:.... Bijvoorbeeld van welke combinaties mogelijk zijn en dat sommig
                ecombinatie natuurlijk
                niets gaan opleveren. bv hier kiezen voor straten en dan zeggen dat je in geonames geinteresseerd bent.
                Of kiezen voor gemeentes en dna verderop nwb kiezen.
                Misschien ook het verschil tussen straten en adressen uitleggen en in welke dataset wat zit?</p>

        </div>

        <div class="col-md-6">

            <h2>Naar...</h2>

            {{ form_row(form.hg_dataset) }}
            <p>uitleg nodig?...</p>

            {{ form_row(form.geometry) }}
            <p>Wilt u een geometrie? Het zoeken gaat een stuk sneller, zonder.</p>

            <div class="row">
                {{ form_widget(form.save) }}
                <div class="alert alert-warning">
                    <p>Bewaar alleen de instellingen en test of standaardiseer later.</p>
                </div>
            </div>

            <div class="row">
                {{ form_widget(form.map) }}
                <div class="alert alert-warning">
                    <p>Deze optie standaardiseert de eerste 20 records. Dit is handig om te kijken of alles goed
                        gaat alvorens het hele bestand te verwerken.
                    </p>
                </div>
            </div>


            <div class="row">
                {{ form_widget(form.mapall) }}
                <div class="alert alert-warning">
                    <p>Deze optie standaardiseert alle records. Dit proces kan enige tijd duren.</p>

                    <p>
                        <strong>Let op:</strong> eventueel eerder opgehaalde data bij dit
                        bestand wordt eerst verwijderd!
                    </p>
                </div>
            </div>
        </div>

        {{ form_end(form) }}

    </div>

{% endblock %}